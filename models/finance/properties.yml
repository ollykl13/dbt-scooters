version: 2

models:
  - name: "revenue_daily"
    description: "Суммарная дневная выручка"  
    config:
      materialized: "incremental"  
      incremental_strategy: "merge"
      unique_key: [ "date" ]  
      on_schema_change: "append_new_columns"
      full_refresh: false
